{
  "name": "library",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Ilia Znamenskii",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "checktypes": "turbo run checktypes",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.10.7"
  }
}
